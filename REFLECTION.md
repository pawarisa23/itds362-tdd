# ทบทวนย้อนหลัง (A7)

## 1. มีเทสต์กี่ตัว โค้ดจริงกี่บรรทัด และมีอัตราส่วนเท่าไหร่

จากที่นับ มีเทสต์ทั้งหมด 8 ตัว ส่วนโค้ดจริงมีประมาณ 38 บรรทัด แบ่งเป็น 3 คลาส ได้แก่ `Quantity`, `Sum` และ `Converter`

คิดเป็นสัดส่วนประมาณ **1 เทสต์ต่อโค้ดจริง 5 บรรทัด**

สิ่งที่สังเกตได้คือไฟล์เทสต์มีความยาวมากกว่าไฟล์โค้ดจริงเกือบเท่าตัว ตอนแรกจึงรู้สึกเหมือนกันว่าอาจกำลังเขียนเทสต์มากเกินความจำเป็นหรือไม่ เพราะตัวโค้ดจริงมีอยู่ไม่มาก

แต่พอถึง A5 ซึ่งต้องเพิ่มหน่วยเข้าไปใน constructor เทสต์เดิมหลายตัวพังพร้อมกัน ตรงนั้นทำให้เห็นชัดว่าเทสต์ที่เขียนไว้มีประโยชน์ เพราะไม่จำเป็นต้องไล่เดาเองว่าการเปลี่ยนแปลงครั้งนี้ส่งผลกระทบกับส่วนไหนบ้าง ตัวเทสต์ช่วยระบุจุดที่มีปัญหาออกมาให้ทันที

อีกอย่างหนึ่งที่เห็นคือเทสต์ 3 ตัวแรกยังวนอยู่กับพฤติกรรมของการคูณเป็นหลัก แต่เทสต์ช่วงหลัง โดยเฉพาะ 2 ตัวสุดท้าย เป็นตัวที่ผลักให้โครงสร้างของโปรแกรมเปลี่ยนไปอย่างชัดเจน จนต้องแยก `Sum` และ `Converter` ออกมาเป็นคลาสของตัวเอง

## 2. ช่วงไหนที่สถานะ RED นานที่สุด และเพราะอะไร

ช่วงที่ RED นานที่สุดคือ A6 ตอนเปลี่ยนให้ `plus` คืนค่าเป็น `Sum`

สาเหตุที่ใช้เวลานานไม่ได้เกิดจากโค้ดแก้ยากโดยตรง แต่เป็นเพราะ error ที่เกิดขึ้นพาไปมองผิดจุด ในตอนนั้น `reduce` ยังเป็น implementation แบบชั่วคราวอยู่ คือรับอะไรมาก็คืนสิ่งนั้นกลับไปทันที

เมื่อ `plus` เริ่มคืนค่าเป็น `Sum` จึงเกิดกรณีที่ระบบนำ `Sum` ไปเปรียบเทียบกับ `Quantity` และสุดท้ายเกิด `AttributeError`

ปัญหาคือ traceback กลับชี้ไปที่บรรทัดใน `__eq__` ซึ่งเป็นส่วนที่ไม่ได้แก้ไขเลยในรอบนั้น ทำให้ช่วงแรกพยายามหาว่าทำไม `__eq__` ถึงมีปัญหา ทั้งที่ต้นเหตุจริงอยู่ที่การทำงานร่วมกันของ `plus` และ `reduce`

กว่าจะย้อนกลับไปตรวจสอบว่า `reduce` คืนค่าอะไรออกมา ก็ใช้เวลาไปพอสมควร

เมื่อมองย้อนหลัง สิ่งที่เกิดขึ้นสะท้อนว่าก้าวในรอบนั้นใหญ่เกินไป เพราะมีการเปลี่ยนชนิดของค่าที่ `plus` คืนกลับมา ทั้งที่ส่วนปลายทางยังไม่สามารถจัดการกับชนิดข้อมูลใหม่นั้นได้ เท่ากับว่าในทางปฏิบัติเป็นการเปลี่ยนหลายเรื่องพร้อมกัน

ถ้าต้องทำใหม่ คิดว่าจะให้ `Sum` รองรับ `reduce` ตั้งแต่ commit เดียวกับที่สร้าง `Sum` ขึ้นมา เพื่อให้การเปลี่ยนแปลงแต่ละรอบมีขอบเขตที่เล็กลง

ในหนังสือมีการกล่าวว่ารอบ RED-GREEN หนึ่งรอบไม่ควรใช้เวลานานเกินประมาณสองนาที ตอนอ่านครั้งแรกค่อนข้างรู้สึกว่าตัวเลขนี้อาจเคร่งเกินไป แต่พอได้เจอสถานการณ์นี้จริงจึงเข้าใจมากขึ้นว่า หากสถานะ RED ค้างอยู่นาน บางครั้งไม่ได้หมายความว่าโจทย์ยาก แต่เป็นเพราะเรากำลังก้าวใหญ่เกินไป

## 3. จุดที่ใช้ Fake It / Triangulate / Obvious Implementation

### Fake It

ใช้ใน A1 โดยเขียน

`self.amount = 600`

แบบ hardcode ไปก่อน

ตอนเขียนค่อนข้างรู้สึกฝืน เพราะรู้อยู่แล้วว่านี่ไม่ใช่ implementation ที่ถูกต้องในระยะยาว แต่สิ่งที่ได้กลับมามีประโยชน์สองอย่าง

อย่างแรกคือทำให้ตรวจสอบได้ว่าระบบพื้นฐานทำงานครบจริง ทั้งการ import การเรียก method และการรันผ่าน `pytest`

อย่างที่สองคือทำให้มีสถานะ GREEN เป็นจุดตั้งต้น ก่อนที่จะค่อย ๆ ปรับ implementation ต่อไป โดยไม่ต้องแก้ทุกอย่างพร้อมกันตั้งแต่แรก

เมื่อมองย้อนกลับไปคิดว่าเป็นการเลือกที่เหมาะสม เพราะถ้ากระโดดไปเขียนสูตรจริงทันทีแล้วเทสต์ยังแดงอยู่ เราจะไม่รู้ชัดว่าเกิดจากสูตรผิด การ import ผิด การตั้งชื่อไฟล์ผิด หรือปัญหาส่วนอื่นของระบบ

### Triangulate

ใช้ใน A2 โดยเพิ่มเทสต์กรณีคูณด้วย 2

เมื่อมีตัวอย่างเพิ่มขึ้น ค่า `600` ที่ hardcode ไว้ไม่สามารถตอบโจทย์ทั้งสองกรณีพร้อมกันได้อีกแล้ว จึงทำให้ต้องเปลี่ยนจากค่าปลอมไปเป็นสูตรที่ทำงานจริง

จุดนี้ทำให้เข้าใจว่า Triangulate ไม่ได้หมายถึงเพียงแค่การเพิ่มเทสต์อีกหนึ่งกรณี แต่ควรเลือกตัวอย่างใหม่ที่ **ขัดกับคำตอบเดิม** เพื่อบังคับให้ implementation ทั่วไปขึ้น

หากเพิ่มเทสต์ใหม่ที่ยังสามารถผ่านได้ด้วยค่าที่ hardcode เดิม ก็จะไม่ได้ช่วยผลักให้โค้ดเปลี่ยนไปในทิศทางที่ถูกต้อง

แนวคิดนี้ถูกนำมาใช้อีกครั้งใน A4 ซึ่งเริ่มจาก fake `__eq__` ให้คืนค่า `True` ก่อน จากนั้นจึงเพิ่มกรณี `!=` เพื่อทำให้ implementation แบบปลอมนั้นใช้ต่อไม่ได้

จากตรงนี้เห็นชัดว่า Fake It และ Triangulate ค่อนข้างทำงานต่อเนื่องกัน หากใช้ Fake It แล้วไม่มีเทสต์ใหม่มาบังคับ ของปลอมก็อาจค้างอยู่ในระบบได้นานกว่าที่ควร

### Obvious Implementation

ใช้ตอนเพิ่ม `__repr__`

ส่วนนี้เขียน implementation ไปโดยตรง ไม่ได้ผ่าน RED-GREEN เนื่องจากพฤติกรรมค่อนข้างตรงไปตรงมา และมีประโยชน์ต่อการ debug โดยตรง

ก่อนเพิ่ม `__repr__` เมื่อเทสต์พังจะเห็นค่าประมาณ

`<kitchen.Quantity object at 0x...>`

แต่หลังจากเพิ่มแล้วสามารถเห็นเป็น

`Quantity(600, 'g')`

ทำให้อ่านผลลัพธ์และหาสาเหตุของปัญหาได้ง่ายขึ้นมาก

เมื่อมองย้อนหลัง คิดว่าการเลือกใช้ Obvious Implementation ในจุดนี้ยังสมเหตุสมผล เพราะเป็นส่วนช่วยในการ debug มากกว่าจะเป็นพฤติกรรมหลักของระบบ

อย่างไรก็ตาม ต้องยอมรับว่ามันเป็นโค้ดที่ไม่มีเทสต์ตัวใดบังคับให้เขียนขึ้นมา ซึ่งถือว่าไม่เคร่งตามหลัก TDD แบบเต็มที่ ถ้าต้องการให้ครบถ้วนจริงก็ควรมีเทสต์รองรับพฤติกรรมของ `__repr__` ด้วยเช่นกัน

## 4. มีรายการใดค้างอยู่ และรายการใดควรลบทิ้ง

รายการเริ่มต้นทั้ง 7 ข้อสามารถทำและขีดออกได้ครบทั้งหมด

ระหว่างการพัฒนามีเรื่องอื่นที่นึกออกเพิ่มเติมหลายอย่าง แต่ตัดสินใจยังไม่ทำ เพราะยังไม่มี requirement ที่ต้องใช้ ได้แก่

* รองรับการแปลงจาก g กลับเป็น oz ซึ่งโครงสร้าง `Converter` สามารถรองรับได้อยู่แล้ว เพียงเพิ่ม conversion rate
* เพิ่มหน่วยอื่น เช่น kg หรือ lb ซึ่งปัจจุบันเก็บอัตราการแปลงไว้ใน `dict` จึงสามารถเพิ่มภายหลังได้ไม่ยาก
* ทำ error message ให้เข้าใจง่ายขึ้นเมื่อผู้ใช้ใส่หน่วยที่ระบบไม่รู้จัก ซึ่งตอนนี้ยังได้ `KeyError`
* ทำให้ `Quantity` สามารถนำไปใช้ใน `set` ได้ เพราะเมื่อกำหนด `__eq__` แล้ว Python จะปิด `__hash__` โดยอัตโนมัติ
* ตรวจสอบใน `__eq__` ว่าค่าที่นำมาเปรียบเทียบเป็น `Quantity` จริงหรือไม่ เพราะปัจจุบันถ้าเปรียบเทียบกับชนิดอื่นจะเกิด `AttributeError` แทนที่จะคืนค่า `False`

สำหรับ 3 ข้อแรก คิดว่า **ควรตัดออกจากสิ่งที่ต้องทำในตอนนี้** เพราะเป็นการคาดเดาความต้องการในอนาคต หากรีบเขียนไว้ก่อนก็จะกลายเป็นโค้ดที่ยังไม่มีผู้ใช้จริง แต่ต้องคอยดูแลต่อไป

ส่วน 2 ข้อหลังคิดว่าควรเก็บไว้ในรายการต่อ เพราะเป็นปัญหาที่พบจากการใช้งานจริงระหว่างทำ ไม่ใช่เพียงการคาดเดา โดยเฉพาะกรณีของ `__eq__` ที่มีส่วนทำให้ error ใน A6 อ่านยากขึ้น

สิ่งที่ได้จากการจดรายการเหล่านี้ไว้โดยยังไม่ลงมือทำทันที คือช่วยให้ไม่ต้องเก็บทุกเรื่องไว้ในหัว และลดโอกาสที่จะหลุดไปพัฒนา feature ที่ยังไม่มีความจำเป็น

## 5. ถ้าออกแบบบนกระดาษก่อน จะคิด `reduce` ออกมาหรือไม่

คำตอบคือ **ไม่น่าจะคิดออกมาในรูปแบบนี้**

ถ้าเริ่มจากการออกแบบบนกระดาษก่อน น่าจะเขียน `plus` ให้บวกค่าตัวเลขแล้วคืน `Quantity` ออกมาโดยตรง

จากนั้นก็น่าจะติดอยู่กับคำถามว่า

`200 g + 1 oz`

ควรคืนค่าเป็นหน่วยอะไร

คำถามนี้ตอบไม่ได้หากยังไม่รู้ว่าผู้เรียกต้องการผลลัพธ์ในหน่วยใด สุดท้ายอาจลงเอยด้วยการเลือกหน่วยตายตัว เช่น แปลงทุกอย่างเป็นกรัมก่อน ซึ่งเมื่อ requirement เปลี่ยนก็มีโอกาสต้องกลับมารื้อโครงสร้างใหม่

สิ่งที่ TDD พาไปเจอคือการแยกแนวคิดระหว่าง **"เก็บไว้ว่าสองสิ่งนี้ต้องถูกบวกกัน"** ในรูปของ `Sum` ออกจาก **"ค่อยลดรูปหรือแปลงให้เป็นหน่วยที่ต้องการ"** ผ่าน `reduce`

ด้วยวิธีนี้ `200 g + 1 oz` สามารถเป็น object ที่ถูกต้องได้ แม้ในตอนนั้นจะยังไม่รู้ว่าผลลัพธ์เชิงตัวเลขสุดท้ายควรเป็นเท่าไร เพราะการคำนวณสามารถรอจนถึงจุดที่มีข้อมูล conversion rate และรู้ว่าต้องการหน่วยอะไร

โครงสร้างนี้ไม่ได้เกิดจากการวางแผนล่วงหน้า แต่เกิดจากรูปแบบของเทสต์ที่ใบงานกำหนดมา เช่น

```python
total = grams(200).plus(grams(300))

converter = Converter()

assert converter.reduce(total, "g") == grams(500)
```

รูปแบบของเทสต์แยก "การบวก" ออกจาก "การเลือกหน่วยของผลลัพธ์" ตั้งแต่ต้น ทำให้โค้ดจริงค่อย ๆ ถูกผลักให้แยกความรับผิดชอบตามไปด้วย

ตรงนี้ทำให้เห็นความหมายของแนวคิดที่ว่า **เทสต์คือผู้ใช้คนแรกของโค้ด** ค่อนข้างชัด เพราะ interface ที่เทสต์ต้องการเป็นตัวกำหนดรูปทรงของโค้ดที่ค่อย ๆ เกิดขึ้น

จุดที่เห็นประโยชน์ชัดที่สุดคือ requirement สุดท้าย

`(200 g + 1 oz) × 2`

โครงสร้างที่มีอยู่สามารถรองรับได้แทบจะทันที เพียงเพิ่ม `times` ให้ `Sum` แล้วกระจายการคูณไปยังทั้งสองฝั่ง

```python
def times(self, multiplier):
    return Sum(
        self.left.times(multiplier),
        self.right.times(multiplier)
    )
```

ตรงนี้ต้องคืนค่าเป็น `Sum` ไม่ใช่ `Quantity` เพราะในขั้นตอนการคูณยังไม่มี `Converter` หรือข้อมูลว่าผู้เรียกต้องการผลลัพธ์ในหน่วยใด จึงยังไม่ควรรีบลดรูปเป็นค่าเดียว

หลังจากทำเสร็จจึงสังเกตว่า ทั้ง `Quantity` และ `Sum` เริ่มมีพฤติกรรมคล้ายกัน คือรองรับทั้ง `times` และ `reduce`

จึงเหมือนเกิด protocol ร่วมกันขึ้นมาโดยไม่ได้ตั้งใจออกแบบไว้ล่วงหน้า แต่ค่อย ๆ ปรากฏจากการทำทีละก้าวตามเทสต์

ถ้าเริ่มจากการออกแบบบนกระดาษตั้งแต่แรก คิดว่ามีโอกาสน้อยที่จะได้โครงสร้างแบบนี้ตั้งแต่ครั้งแรก และอาจต้องกลับมาปรับโครงสร้างครั้งใหญ่เมื่อเจอ requirement ข้อสุดท้าย

# เรื่องอื่นที่สังเกตระหว่างทำ

## ใบงานมี 2 จุดที่ลำดับ RED-GREEN ไม่ตรงกับสิ่งที่เกิดขึ้นจริง

ระหว่างทำตามใบงานพบ 2 จุดที่ขั้นตอนซึ่งระบุว่าเป็น GREEN กลับยังเกิด test failure จริง จึงปรับลำดับการทำเล็กน้อยเพื่อรักษากฎว่า **"แก้เทสต์เฉพาะตอนที่ระบบอยู่ในสถานะ GREEN"**

### A3

หากเปลี่ยน `times` ให้คืน object ใหม่ก่อน เทสต์ A1 และ A2 จะพังทันที เพราะเทสต์เดิมทั้งสองตัวเรียก

`flour.times(n)`

แล้วกลับไปอ่านค่า `flour.amount` จาก object เดิม ซึ่งหลังจากเปลี่ยน implementation แล้วจะไม่ถูกแก้ค่าอีก

ผลที่เกิดขึ้นคือ `2 failed, 1 passed` ทั้งที่ในใบงานระบุช่วงดังกล่าวว่าเป็น GREEN และให้แก้เทสต์ต่อในขณะที่ระบบเขียว

จึงเลือกปรับเทสต์ทั้ง 3 ตัวให้เข้าสู่สถานะ RED ในครั้งเดียวก่อน แล้วจึงแก้ implementation เพื่อทำให้กลับมา GREEN อีกครั้ง

### A5

กรณีนี้หากแก้ `__init__` ให้บังคับรับ `unit` ทันที เทสต์เดิมจะพัง 4 จาก 5 ตัวด้วย `TypeError` เนื่องจาก call site เก่ายังไม่ได้ส่งหน่วยเข้ามา

จึงเลือกใส่ค่า default เป็น

`unit="g"`

ชั่วคราวก่อน เพื่อให้สามารถทยอยย้าย call site ไปใช้ `grams()` และ `ounces()` ได้ในขณะที่ test suite ยังคงเป็น GREEN

หลังจากเปลี่ยน call site ครบแล้วจึงค่อยนำ default ออกในภายหลัง วิธีนี้ทำให้แต่ละ commit ในช่วง migration ยังคงอยู่ในสถานะ GREEN

จากการเจอจุดลักษณะนี้เองทำให้เข้าใจมากขึ้นว่าทำไมกฎ **"แก้เทสต์เฉพาะตอน GREEN"** จึงสำคัญ

หากแก้ทั้งเทสต์และ production code พร้อมกันในขณะที่ระบบยัง RED เมื่อรันแล้วไม่ผ่านต่อ เราจะเริ่มแยกได้ยากว่าปัญหาเกิดจากเทสต์ที่แก้ใหม่ หรือเกิดจาก implementation ที่ยังไม่ถูกต้อง

## ขนาดของแต่ละก้าวสะท้อนความซับซ้อนของงานได้ค่อนข้างชัด

เมื่อดูจากจำนวน commit ในแต่ละขั้น จะเห็นความแตกต่างได้ชัดเจน

A1 และ A2 ใช้ประมาณ 2 commit ต่อขั้น ขณะที่ A6 ใช้ถึง 8 commit เพราะต้องวน RED-GREEN ประมาณ 4 รอบ ได้แก่

`fake → แยก Sum → เพิ่ม conversion rate → กระจายการคูณ`

ตอนแรกมองว่าการแบ่งงานย่อยขนาดนี้อาจทำให้เสียเวลาโดยไม่จำเป็น แต่หลังจากเจอ `AttributeError` ที่ชี้ไปผิดจุดในช่วง A6 ก็เห็นประโยชน์ชัดขึ้น

หากรวมการเปลี่ยนแปลงทั้งหมดไว้ในครั้งเดียว เมื่อเกิด error ขึ้นจะมีพื้นที่ให้ค้นหาสาเหตุกว้างกว่ามาก และน่าจะใช้เวลาแก้ปัญหานานกว่าการค่อย ๆ เปลี่ยนทีละส่วน

ดังนั้นสิ่งที่ได้จากรอบนี้คือ ขนาดของก้าวใน TDD ไม่ได้มีผลแค่เรื่องความเป็นระเบียบของ commit แต่ช่วยจำกัดขอบเขตของปัญหา ทำให้เมื่อเทสต์พังสามารถย้อนกลับไปหาสาเหตุได้ง่ายขึ้นด้วย
